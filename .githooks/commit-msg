#!/bin/sh
# Strip Cursor co-author trailers so cursoragent does not appear on GitHub
grep -viE "^Co-authored-by: (Cursor|cursoragent)" "$1" | grep -vi "cursoragent@cursor.com" > "$1.tmp" && mv "$1.tmp" "$1"
exit 0
