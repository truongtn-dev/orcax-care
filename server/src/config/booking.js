/** Default consultation fee (VND) when doctor has no custom fee configured. */
export const DEFAULT_CONSULTATION_FEE_VND = 200000;

/** How many days ahead patients can browse availability by default. */
export const DEFAULT_AVAILABILITY_HORIZON_DAYS = 14;

/** Maximum date range for availability queries. */
export const MAX_AVAILABILITY_RANGE_DAYS = 31;
