# Mobile (React Native)

Add the React Native app here when the team starts the mobile track (Expo or RN CLI). Keep versions aligned with `client` and `server` API contracts.

Until then, this folder documents intent only.
